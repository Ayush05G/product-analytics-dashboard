# Copy to .streamlit/secrets.toml (git-ignored) for local use, or paste into
# Streamlit Community Cloud -> app Settings -> Secrets.
#
# Direct download URL for the RetailRocket events.csv. Used only when
# data/events.csv is not present locally. Supports a plain .csv or a .csv.gz.
EVENTS_URL = "https://example.com/path/to/events.csv"
